/**
 * sol-form-bundle.js — bundle entry: the RDF editing stack in one import.
 *
 * The web-tier name for core/rdf-bundle.js (which remains the module the
 * `rdf-bundle` importmap alias and the data-edit-shape capability load):
 * solid-logic + solid-ui + sol-tree-edit + sol-form + sol-modal +
 * sol-settings + the edit placements. One import, forms everywhere.
 */
import '../core/rdf-bundle.js';
